import BackButton from './BackButton';

export default {
  component: BackButton,
  title: 'Back Button',
  tags: ['autodocs'],
};

export const Default = {
  args: { onClick: { action: 'clicked back button' } },
};
